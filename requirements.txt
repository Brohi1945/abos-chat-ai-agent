livekit-agents>=1.0
livekit-plugins-groq
livekit-plugins-silero
livekit-plugins-noise-cancellation
edge-tts
python-dotenv

# Uncomment when switching TTS back to Azure (official, same Asad/Uzma
# voices, needs AZURE_SPEECH_KEY + AZURE_SPEECH_REGION):
# livekit-plugins-azure
